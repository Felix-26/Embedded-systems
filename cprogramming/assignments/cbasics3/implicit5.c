#include<stdio.h>

int main() {
	int x = 5;
	if(x) {				// x -> bool (non-zero number becomes true , zero becomes false.) .Seen in if,while,switch,for conditions.
		printf("x is a non zero number.");
	}
	return 0;
}
